import React, { useState, useEffect } from 'react';
import {
  StyleSheet,
  View,
  Text,
  StatusBar,
  Image,
  TouchableOpacity,
  TouchableHighlight,
  Dimensions,
  TextInput,
  Picker,
  Modal,
} from 'react-native';

export const User = ({navigation}) => {
    <View style={{flexDirection: 'row'}}>

    </View>
}